# dbt 1.8: package deps (no Jinja). Replaces packages.yml to avoid schema validator error.
packages:
  - package: dbt-labs/dbt_utils
    version: "1.1.1"
  - package: calogica/dbt_expectations
    version: "0.10.1"

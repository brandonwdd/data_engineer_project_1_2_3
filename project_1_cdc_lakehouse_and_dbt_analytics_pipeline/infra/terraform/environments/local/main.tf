# Local environment: No AWS resources, use MinIO
# This is a placeholder - no actual resources to create

terraform {
  required_version = ">= 1.5.0"
}

# Local environment uses MinIO (via Docker Compose)
# No Terraform resources needed


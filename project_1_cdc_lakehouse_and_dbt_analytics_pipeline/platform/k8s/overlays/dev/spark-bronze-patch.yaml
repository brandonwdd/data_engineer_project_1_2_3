# Patch: Spark Bronze resources (dev)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-bronze-ingest
spec:
  template:
    spec:
      containers:
      - name: spark
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
